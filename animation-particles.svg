<svg fill="none" viewBox="0 0 800 400" width="800" height="400" xmlns="http://www.w3.org/2000/svg">
  <foreignObject width="100%" height="100%">
    <div xmlns="http://www.w3.org/1999/xhtml">
      <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;700&display=swap');

        :root {
          --custom-red: #FF0000;
          --custom-dark-blue: #112957;
          --custom-light-blue: #7de9ff;
          --text-shadow-color: rgba(0, 0, 0, 0.1);
        }

        @keyframes rotateText {
          0% { transform: rotate(3deg); }
          100% { transform: rotate(-3deg); }
        }

        @keyframes gradientBackground {
          0% { background-position: 0% 50%; }
          50% { background-position: 100% 50%; }
          100% { background-position: 0% 50%; }
        }

        @keyframes animateCircle {
          0% { transform: translateY(0) rotate(0deg); opacity: 1; border-radius: 0%; }
          100% { transform: translateY(-1000px) rotate(720deg); opacity: 0; border-radius: 50%; }
        }

        .container-wrapper {
          width: 100%;
          height: 100%;
          display: flex;
          justify-content: center;
          align-items: center;
          padding: 16px; /* Equivalent to p-4 */
          box-sizing: border-box; /* Ensure padding is included in width/height */
        }

        .container {
          font-family: 'Inter', system-ui, -apple-system, "Segoe UI", Roboto, Helvetica, Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji";
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: center;
          width: 100%;
          max-width: 760px; /* Equivalent to max-w-[760px] */
          height: 400px;
          background: linear-gradient(-45deg, var(--custom-red), var(--custom-dark-blue), var(--custom-light-blue));
          background-size: 300% 200%;
          animation: gradientBackground 10s ease infinite;
          border-radius: 10px;
          color: white;
          text-align: center;
          overflow: hidden;
          position: relative; /* For absolute positioning of circles */
        }

        h1 {
          width: 90%;
          font-size: 50px; /* Default font size */
          line-height: 1.3;
          letter-spacing: 3px;
          text-transform: uppercase;
          text-shadow: 0 1px 0 var(--custom-light-blue), 0 2px 0 #efefef, 0 3px 0 #efefef, 0 4px 0 #efefef, 0 12px 5px var(--text-shadow-color);
          animation: rotateText 1s ease-in-out infinite alternate;
          max-width: 100%;
          word-break: break-word;
        }

        /* Responsive font sizes for h1 */
        @media (min-width: 465px) { /* Equivalent to sm: */
          h1 { font-size: 50px; }
        }
        @media (min-width: 640px) { /* Equivalent to md: */
          h1 { font-size: 60px; }
        }
        @media (min-width: 768px) { /* Equivalent to lg: */
          h1 { font-size: 70px; }
        }
        @media (min-width: 1024px) { /* Equivalent to xl: */
          h1 { font-size: 80px; }
        }

        .break-sm-hidden {
          display: none; /* Hide <br> by default */
        }
        @media (max-width: 464px) { /* Show <br> only on very small screens */
          .break-sm-hidden {
            display: block;
          }
        }

        .circles {
          position: absolute;
          bottom: -75px;
          left: 0;
          width: 100%;
          height: 100%;
          pointer-events: none;
          margin: 0; /* Remove default ul margin */
          padding: 0; /* Remove default ul padding */
          list-style: none; /* Remove default list bullets */
        }

        .circles li {
          position: absolute;
          display: block;
          background: rgba(0, 0, 0, 0.125);
          animation: animateCircle linear infinite;
        }

        /* Individual circle properties */
        .circles li:nth-child(1) { left: 10%; width: 20px; height: 20px; animation-delay: 0s; animation-duration: 15s; }
        .circles li:nth-child(2) { left: 5%; width: 15px; height: 15px; animation-delay: 2s; animation-duration: 10s; }
        .circles li:nth-child(3) { left: 60%; width: 15px; height: 15px; animation-delay: 4s; animation-duration: 15s; }
        .circles li:nth-child(4) { left: 30%; width: 40px; height: 40px; animation-delay: 0s; animation-duration: 18s; }
        .circles li:nth-child(5) { left: 50%; width: 15px; height: 15px; animation-delay: 0s; animation-duration: 15s; }
        .circles li:nth-child(6) { left: 75%; width: 60px; height: 60px; animation-delay: 3s; animation-duration: 15s; }
        .circles li:nth-child(7) { left: 35%; width: 75px; height: 75px; animation-delay: 7s; animation-duration: 15s; }
        .circles li:nth-child(8) { left: 50%; width: 15px; height: 15px; animation-delay: 15s; animation-duration: 45s; }
        .circles li:nth-child(9) { left: 20%; width: 10px; height: 10px; animation-delay: 2s; animation-duration: 35s; }
        .circles li:nth-child(10) { left: 85%; width: 75px; height: 75px; animation-delay: 0s; animation-duration: 11s; }
      </style>
      <div class="container-wrapper">
        <div class="container">
          <h1>WEB <br class="break-sm-hidden" /> DEVELOPER💻</h1>
          <ul class="circles">
            <li></li>
            <li></li>
            <li></li>
            <li></li>
            <li></li>
            <li></li>
            <li></li>
            <li></li>
            <li></li>
            <li></li>
          </ul>
        </div>
      </div>
    </div>
  </foreignObject>
</svg>
